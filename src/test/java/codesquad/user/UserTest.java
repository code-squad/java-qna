package codesquad.user;

import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;

public class UserTest {
//    @Test
//    public void 유저수정하기() {
//        List<User> users = new ArrayList<>();
//        users.add(new User("Lime", "123", "라임", "example@naver.com"));
//        users.add(new User("Lime", "1234", "변형된라임", "example2@naver.com"));
//
//        users.get(0).update(users.get(1));
//        assertThat(users.get(0).getPassword()).isEqualTo("1234");
//        assertThat(users.get(0).getName()).isEqualTo("변형된라임");
//        assertThat(users.get(0).getEmail()).isEqualTo("example2@naver.com");
//    }
}
